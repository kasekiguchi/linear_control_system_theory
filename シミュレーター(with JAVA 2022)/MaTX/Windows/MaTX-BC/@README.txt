
MaTXのインストールに必要なものは

  MaTX-x.y.z.exe    : MaTX本体(x.y.zはバージョン番号) (必須)
  MaTX-help.exe     : MaTXのマニュアル                (必須)
  MaTX-util-src.exe : ユーティリティのソース          (選択)

です。それぞれは、自己解凍形式のファイルです。解凍し、
setup.exe を実行すると，インストーラーが起動します。

